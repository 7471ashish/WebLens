services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: weblens-backend
    ports:
      - "8000:8000"
    environment:
      - MAX_CONCURRENT_JOBS=3
      - JOB_TIMEOUT_SECONDS=260.0
      - AGENT_SOURCE_DIR=/app/agent
    restart: unless-stopped
